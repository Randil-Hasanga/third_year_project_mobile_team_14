

import 'package:flutter/material.dart';

class JobProviderPage extends StatelessWidget {
  const JobProviderPage({Key? key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Job Provider Page"),
      ),
      body: const Center(
        child: Text("Welcome to Job ProviderPage"),
      ),
    );
  }
}
